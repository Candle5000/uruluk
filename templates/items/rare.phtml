<?= $this->fetch('header.phtml') ?>
<div class="row flex-xl-nowrap">
  <div class="col-12 col-md-3 col-xl-2 order-xl-1 align-self-start">
  </div>
  <div class="d-none d-xl-block col-xl-2 order-xl-3 align-self-start">
  </div>
  <main class="col-12 col-md-9 col-xl-8 py-md-3 pl-md-5 order-xl-2" role="main">
    <h2>Rare Items</h2>

    <?php if (empty($items)) : ?>
      <h3>No Data</h3>
    <?php endif; ?>
    <?php foreach ($items as $item) : ?>
      <div class="card mb-1 bg-dark">
        <div class="row">
          <div class="col-12 col-md-3 text-center my-1 my-md-3">
            <img src="/img/item/<?= $item['image_name'] ?>" alt="<?= $item['name_en'] ?>" />
          </div>
          <div class="col-12 col-md-9">
            <div class="text-center text-md-left text-primary font-weight-bold">
              <?= $item['name_ja'] ?><br />
              <?= $item['name_en'] ?>
            </div>
            <ul class="list-unstyled text-white ml-2">
              <?php foreach ($item['attributes'] as $attr) : ?>
                <li><?= $attr['short_name'] ?> <?= $attr['attribute_value'] ?></li>
              <?php endforeach ?>
            </ul>
          </div>
        </div>
      </div>
    <?php endforeach ?>
  </main>
</div>
<?= $this->fetch('footer.phtml', ['pv_today' => $pv_today, 'pv_yesterday' => $pv_yesterday]) ?>
